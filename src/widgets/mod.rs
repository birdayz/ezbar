//! Host-side view components. `graph`/sparkline moved into the SDK
//! (`ezbar_plugin::ui`) so modules — built-in and third-party — share it. What
//! remains here are bar-internal views still being migrated to `Module`s.

pub mod workspaces;
